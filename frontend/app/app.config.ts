export default defineAppConfig({
  ui: {
    colors: {
      primary: 'sky',
      neutral: 'slate'
    }
  },
  // Default to light mode for friendly, inviting appearance
  colorMode: {
    preference: 'light'
  }
})
